# Advisor Implementation Plans — FarmPass

Generated by the improve skill audit on 2026-09-23 (HEAD `8ea30ba`). Focus: critical bugs and security, batch 1 — everything PayMongo/demo-related has been excluded by the operator as by-design.

Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Profile save must not overwrite role / verification status | P1 | S | — | DONE |
| 002  | Payment endpoints: explicit authz allowlist | P1 | S | — | DONE |
| 003  | Dashboard endpoints: role guards | P1 | S | — | DONE |
| 004  | JWT refresh: persist rotated refresh, update zustand, single-flight | P1 | S | — | DONE |
| 005  | Release path: lock rows and re-check inside the transaction | P1 | M | 006 (file overlap; land in same session) | DONE |
| 006  | Release queueing on `on_commit` + HogSurvey deduction must not be swallowed | P1 | M | — | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- 005 and 006 both edit `backend/apps/payment/services.py` (005 also edits `permits/services/application.py` via 006's HogSurvey change). Land them in the same working session — sequence 006 before 005 to avoid merge friction — and run the combined payment+permits test suite once at the end.
- No other cross-plan dependencies. 001–004 are independent of each other.

## Findings considered and rejected / deferred

Rejected as by-design (operator decision — the PayMongo flow is a capstone demo):
- "Pay Online" hardwired to the sandbox modal (`PaymentCheckout.jsx:66-70`).
- No server-side PayMongo webhook; release depends on farmer polling.
- DEBUG-gated payment simulation endpoints and "active-as-paid" verification.
- Fee cent-conversion truncation (`int(fee)` vs `int(fee*100)`) — only reachable via the demo gateway path.
- Payment stats miscount for method `ONLINE` — stems from demo checkout sessions.

Deferred (real but not in batch 1; candidate batch 2):
- Public self-registration not bound to OTP; phone-owner enumeration (`api/services.py:37`).
- Systemic over-broad object visibility (all applications/OCR/audit trail/farmer rosters to any authenticated role).
- OTP has no attempt limit; prod uses per-worker LocMemCache.
- Unauthenticated `/media/` serving + `X-Frame-Options` stripped for documents.
- `str(e)` exception detail leakage to clients.
- Client-supplied `permit_fee` on issuance bypasses `MunicipalConfig`.
- Barangay CRUD + HogSurvey writes open to all roles.
- AIC number minted twice per permit (`numbers.py` ignores `instance`).
- Status-SMS dedup suppresses legitimate repeat transitions.
- `verify_permit` / inspector log lack a server-side paid/RELEASED guard.